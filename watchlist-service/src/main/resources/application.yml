spring:
  application:
    name: watchlist-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

data:
    redis:
        host: localhost
        port: 6379

kafka:
    bootstrap-servers: localhost:9092